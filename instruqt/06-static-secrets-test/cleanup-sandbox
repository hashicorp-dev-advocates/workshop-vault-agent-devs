#!/bin/bash
set -euxo pipefail

# Stop vault-agent and the Spring Boot application before the next challenge
cd /root/workshop-vault-agent-devs
docker compose stop vault-agent
pkill -f "spring-boot:run" || true
