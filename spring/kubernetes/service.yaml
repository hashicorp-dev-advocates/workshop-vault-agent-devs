apiVersion: v1
kind: Service
metadata:
  name: payments-app
  namespace: default
  labels:
    app: payments-app
spec:
  type: ClusterIP
  selector:
    app: payments-app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
